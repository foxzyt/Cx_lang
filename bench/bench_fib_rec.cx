// perf-rider: recursive call-depth workload — naive fibonacci
// (JIT-lowerable surface; exercises call/return + branch).
fnc: t64 fib(n: t64) {
    if (n < 2) {
        return n
    }
    fib(n - 1) + fib(n - 2)
}
print(fib(27))
