// CR#4: a branch-tail literal rejects when the value initializes a struct field
// of declared width (struct-literal site converged onto the shared width check).
struct S { a: t8 }
s: S = S { a: if true { 300 } else { 5 } }
print(s.a)
