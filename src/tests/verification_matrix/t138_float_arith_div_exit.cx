// CX-103: exit-code-based JIT parity — f64 division.
// Results cast to t32 for assert_eq (f64 not yet supported by assert_eq).
a: f64 = 21.0
b: f64 = 3.0
quotient: f64 = a / b
n: t32 = quotient
assert_eq(n, 7)

x: f64 = 10.0
y: f64 = 2.0
quotient2: f64 = x / y
m: t32 = quotient2
assert_eq(m, 5)
