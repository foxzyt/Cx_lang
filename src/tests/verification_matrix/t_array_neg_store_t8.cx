// Gate-2a: standalone t8 array with a negative element. Silently miscompiled
// before (the 8-byte store overran the 3-byte array but happened not to crash
// by stack-layout luck — no fixture caught it). Now narrowed to a 1-byte store;
// reads back -128.
a: [3: t8] = [-128, 0, 127]
print(a:[0])
