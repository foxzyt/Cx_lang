// Interpreter fix: same gap as t_f64_comparison_gt.cx, GtEq operator, equal
// operands (the boundary case). Reference-side (interpreter) bug only — the
// JIT was already correct before this fix and should show no behavior
// difference here.
print(3.14 >= 3.14)
