// CX-103: exit-code-based JIT parity — f64 subtraction.
// Results cast to t32 for assert_eq (f64 not yet supported by assert_eq).
a: f64 = 10.0
b: f64 = 3.0
diff: f64 = a - b
n: t32 = diff
assert_eq(n, 7)

x: f64 = 5.5
y: f64 = 0.5
diff2: f64 = x - y
m: t32 = diff2
assert_eq(m, 5)
