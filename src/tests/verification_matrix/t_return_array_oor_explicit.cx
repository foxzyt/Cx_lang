// CR#3: explicit `return` of an array also range-checks elements (the explicit
// path lacked the element check before CR#3; the shared chokepoint adds it).
fnc: [3: t8] f() { return [1, 2, 300] }
print(0)
