fnc: Result<t32> parse(x: t32) {
    if x < 0 {
        return Err("negative")
    }
    return Ok(x)
}

fnc: Result<t32> process(x: t32) {
    if x == 0 {
        return Ok(0)
    }
    val = parse(x)?
    return Ok(val * 2)
}

print(process(5))
print(process(-1))
