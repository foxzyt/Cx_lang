# Cx Daily Log — 2026-05-03

## Snapshot

Light day. The only real code activity was PR #53 merging a documentation-only commit into submain — block-level comments explaining the unary lowering strategy in `lower.rs`. The site branch also received a blog-format dev log for 2026-05-01. No new features, no test changes, no movement on the submain-to-main merge. Matrix holds steady at 78/78 on main.

## Landed today

- **CONFIRMED** — `f0b3ee5` on `origin/submain` (2026-05-03 16:07 EDT): Merge of PR #53 from `stokowski/cx-6-document-unary-lowering`. This brought commit `2d665a4` (created yesterday) into submain. The commit adds 25 lines of block-level comments to `src/ir/lower.rs` documenting why unary operations are lowered as two-operand forms: `Op::Minus` becomes `0 - value` (no dedicated negate instruction), `Op::Not` becomes `value == 0` (Bool equality against zero). No behavioral change — documentation only.

- **CONFIRMED** — `b83665c` on `origin/site` (2026-05-03 00:16 UTC): A blog-format dev log for 2026-05-01 was added to `src/content/blog/2026-05-01.mdx`. This summarizes the Phase 11 sub-packets (IR memory ops and struct registry) that landed on submain May 1st.

Neither commit affects main. No features shipped. No tests added or changed.

## In progress / uncommitted work

Nothing to report. Working tree is clean (detached HEAD on main). No staged, unstaged, or untracked changes. No recently modified files in `src/`, `docs/`, or `.claude/worktrees/` newer than the roadmap.

## Decisions and direction

**Documentation-first for IR lowering.** PR #53 signals continued investment in documenting the encoding choices within the lowering pass before advancing to the next implementation sub-packet. The unary lowering comments explain that the IR deliberately avoids unary instructions — backends only need to handle one instruction shape (two-operand). This is a small but meaningful architectural decision being recorded in-code.

**Submain divergence: day 36.** Submain carries 25 commits not on main (including the new merge commit). The `stokowski` branch was forked from submain, and PR #53 targeted submain — confirming that active development continues on submain while main accumulates only daily-log merges. Main is at v4.9 / 78 tests; submain is at v5.0 / 117 tests with all hard blockers resolved.

## Roadmap updates

- **No items checked off.** Today's work was documentation, not feature completion.
- **Minor edit to Active section:** Added a note that the CX-6 documentation PR (#53) was merged to submain on 2026-05-03, documenting unary lowering strategy in `lower.rs`.
- **No new tasks added.** Existing entries already cover the natural next steps.

## What's next

**Yesterday's predictions vs. what happened:**

1. "Merge submain to main" — did NOT happen. Now ~36 days diverged.
2. "Land CX-6 documentation PR" — YES. PR #53 was merged to submain today. This was predicted and confirmed.
3. "Struct field access lowering" — did NOT happen.
4. "Struct literal lowering" — did NOT happen.

**Next steps, ranked by leverage:**

1. **Merge submain to main.** 25 commits diverged, ~36 days. This would bring main from v4.9 to v5.0, close a 39-test gap (78 vs 117), and resolve all hard blockers on the main roadmap. Remains the single highest-leverage action available.
2. **Struct field access lowering.** The struct registry, Ptr type, and Alloca/Load/Store infrastructure are in place on submain. Lowering `DotAccess` using field offsets from `StructLayoutInfo` is the natural next Phase 11 sub-packet.
3. **Struct literal lowering.** `SemanticExprKind::StructLit` can lower to Alloca + per-field Store instructions using layout offsets.

---

**Matrix status:** 78 PASS / 0 FAIL / 78 total on main. Unchanged from yesterday.

**Evidence sources used:** `git fetch --all --prune`, `git log --all --since="24 hours ago"` (all commits, author-filtered, merge/non-merge), `git show f0b3ee5` and `git show 2d665a4` (PR #53 merge and its source commit), `git show b83665c` (site branch dev log), `git log origin/submain --not origin/main` (25 branch-local commits), `git status`, `git diff` (all variants), `git ls-files --others`, `find` for recently modified files, `run_matrix.sh` output (78/78), yesterday's daily log from `origin/daily-log-2026-05-02`, roadmap at `docs/frontend/ROADMAP.md` (v4.9).
