# Cx Daily Log — 2026-05-19

## Snapshot

A narrow day. No new code commits landed in the last 24 hours. The meaningful activity was infrastructure: the May 18 daily log was written and pushed (but its PR was not merged to main), `run_matrix.sh` got an execute-permission fix, and a blog post for the May 9 dev log was published to the site branch. The project state remains the same as end-of-day May 18: CX-233 (while-in loop IR lowering) is branch-local on `stokowski/CX-233` awaiting merge to submain, and submain sits 171 commits ahead of main with no merge in sight.

## Landed today

**Site blog post for 2026-05-09 (23672a9, Claude, May 19 00:05 UTC).** CONFIRMED. Published to `origin/site` as `src/content/blog/2026-05-09.mdx` (56 lines). This is a content publishing action, not code work.

**run_matrix.sh execute permission fix (86901ce, Claude, May 18 23:41 UTC).** CONFIRMED. Added execute permission to `src/tests/run_matrix.sh` on the `daily-log-2026-05-18` branch. Infrastructure fix from the previous daily log session.

**Daily log 2026-05-18 (1c2bd98, Claude, May 18 23:40 UTC).** CONFIRMED. The May 18 daily log was committed and pushed to `origin/daily-log-2026-05-18` but the PR was not merged to main. That log documented the day's real work: CX-233 (while-in IR lowering), CX-228 (19 parity fixtures), and PR #268 (backend-determinism train → submain merge).

No new code work landed in the last 24 hours across any branch.

## In progress / uncommitted work

No uncommitted changes. Working tree is clean (HEAD detached at main).

The significant in-progress work is the CX-233 branch (`origin/stokowski/CX-233`), committed May 18 at 12:32 UTC — outside today's 24-hour window but still branch-local and awaiting review/merge. CX-233 implements while-in loop source-to-IR lowering: a 307-line change across `src/ir/lower.rs`, `src/frontend/semantic.rs`, `src/frontend/semantic_types.rs`, and the parity checklist. It adds `lower_while_in_single` (5-block CFG: entry → header → body → increment → exit), threads `arr_binding`/`arr_ty` through the semantic layer, and adds `Op::Mul` cursor-deref unary lowering. WhileLoop parity moves from 6 PASS / 2 SKIP to 8 PASS / 0 SKIP.

The daily-log-2026-05-18 PR also remains unmerged, leaving the May 18 log and roadmap updates orphaned on their branch.

## Decisions and direction

No new technical decisions today. The project direction remains as set by the May 18 work:

- **While-in loop is the last piece of loop construct coverage.** Once CX-233 merges to submain, all loop variants (while, for, while-in, loop/break/continue) will have full source-to-IR lowering. The remaining unsupported statement kinds are `When` (match/when blocks) and `EnumDef`.
- **Submain → main gap at 171 commits.** This has been flagged in every daily log since May 6. The gap has grown from 16 (May 7) → 40 (May 9) → 164 (May 16) → 171 (May 18) → 171 (today, unchanged). The stall is purely merge latency — submain continues to accumulate new work without syncing to main.

## Roadmap updates

The roadmap on main was last updated May 9. The May 18 daily-log branch carried an updated roadmap but it was never merged. Today's roadmap update incorporates the May 18 working note and adds a May 19 note. No items were checked off — CX-233 is still branch-local and cannot be marked complete. Phase 12 (differential harness) status was updated to reflect the CX-228 fixture expansion.

No new tasks were added. The existing unchecked items accurately reflect the remaining work.

## What's next

**May 18 predictions vs. what happened today:**

1. "Merge CX-233 into submain" — NOT YET. Still branch-local. This is the most obvious next action.
2. "Submain → main merge" — NOT YET. Gap unchanged at 171. Every daily log since May 6 has predicted this.
3. "Continue Phase 15 parity expansion" — NO NEW WORK. No new parity fixtures or instruction lowering today.
4. "Phase 9 print/println/printn" — NO PROGRESS.

**Likely next moves:**

1. **Merge CX-233 into submain.** The while-in lowering is complete and tested. This is a clean single-commit PR that would close WhileLoop parity at 8/0.
2. **Merge daily-log-2026-05-18 to main** (or close and supersede with today's log).
3. **Submain → main merge.** At 171 commits ahead and growing, this is critical. The integration risk increases with every day it doesn't happen.
4. **Phase 15 next parity targets.** With WhileLoop at 0 SKIP (pending CX-233 merge), the next parity gaps are VariableDecl, DirectCall, InfiniteLoop, and BuiltinAssert categories per the parity checklist.

---

**Matrix status:** 117 PASS / 0 FAIL out of 117 total. Stable — unchanged since at least May 9.

**Submain gap:** 171 commits ahead of main (unchanged from May 18). CX-233 is 172 commits ahead (1 beyond submain).

**Evidence sources:** `git fetch --all --prune`, `git log --all --since="24 hours ago"` (3 commits: 23672a9, 86901ce, 1c2bd98 — all by Claude, no code work), `git log --all --since="48 hours ago"` (adds c5dc3d9 CX-233 and 1ff0901 CX-228 from May 18), `git diff origin/submain..origin/stokowski/CX-233` (304 insertions, 63 deletions across 4 files), `git rev-list origin/main..origin/submain --count` (171), `run_matrix.sh` (117/117), `daily_log/2026-05-18.md` from `origin/daily-log-2026-05-18`, `docment/ROADMAP.md`.
